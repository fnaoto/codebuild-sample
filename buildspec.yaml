version: 0.2 
phases:
  build:
    commands:
      - env
      - |
        # export bin PATH
        export PATH=$PATH:$(pwd)/bin

        if trigger is release ?; then
          notify "<!here> ${CODEBUILD_WEBHOOK_TRIGGER#tag/}"
        fi
      - |
        docker run -t test alpine
